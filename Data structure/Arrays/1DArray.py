# Python doesn't have built-in support for arrays, but Python Lists can be used instead.
# Arrays are not supported in python, so lists were used

list=[]
for i in range(5):
    list.append(int(input("Enter number")))

print(list)